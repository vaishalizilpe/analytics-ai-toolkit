# Runtime dependencies.
# ~= (compatible release) allows patch updates within the same major.minor,
# preventing silent breaking changes from major version bumps.
anthropic~=0.25
openai~=1.30
google-generativeai~=0.7
streamlit~=1.32
scipy~=1.12
numpy~=1.26
pandas~=2.2
plotly~=5.20
python-dotenv~=1.0
